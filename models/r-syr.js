const {Schema,model} = require("mongoose");
module.exports = model("rsyr", new Schema({
    user: String,
        name: String,
    haw: Number,
  lo7h: String,
  no3: String,
    guildId: String
})
                       )
